the file in src/stores/modules are all getting red warning by IDE, it may because I upgraded the vue version, fix them without downgrading the vue version:
msg: Vue: Expected 2-3 arguments, but got 1
function: defineStore
